<h2>Sign in</h2>

<%= form_for(resource, :as => resource_name, :class =>"form-horizontal", :url => session_path(resource_name)) do |f| %>
  <div><%= f.label :email, :class=>"control-group" %>
  <%= f.email_field :email, :class=>"controls" %></div>

  <div><%= f.label :password , :class=>"control-group"%>
  <%= f.password_field :password, :class=>"controls" %></div>

  <% if devise_mapping.rememberable? -%>
    <div class="form-inline"><%= f.check_box :remember_me %> <%= f.label :remember_me %></div></br>
  <% end -%>

  <div><%= f.submit "Sign in", :class=>"btn btn-primary" %></div>
<% end %>

<%= render :partial => "devise/shared/links" %>